project 'On Board Sales - New Fix.xcodeproj'

platform :ios, '9.0'

target 'On Board Sales - New Fix' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for On Board Sales - New Fix
  pod 'RealmSwift'

end
